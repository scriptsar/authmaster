

import supertest from "supertest";
import app from "../../app";
// hnadle db if needed


const api=app;


describe('User Authentication endPoints',()=>{

    test('should return valid data', () => { second })
    test('should return a token', () => { second })
    test('should return 400 if invalid data', () => { second })
    test('should return valid data', () => { second })
})